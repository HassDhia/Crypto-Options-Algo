admin:
  existingSecret: grafana-admin
datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        url: http://kube-prom-stack-prometheus.monitoring:9090
        access: proxy
        isDefault: true
